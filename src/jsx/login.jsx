define(["bus"], function(Bus) {
    var Login = React.createClass({
        render: function() {
            return (
                <p>Login</p>
            );
        }
    });
    return Login;
});
